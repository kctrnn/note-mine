import React from 'react';
import PropTypes from 'prop-types';
import MainPage from './pages/MainPage';

const Note = (props) => {
  return <MainPage />;
};

Note.propTypes = {};

export default Note;
