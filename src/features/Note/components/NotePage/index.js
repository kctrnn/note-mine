import React from 'react';
import PropTypes from 'prop-types';
import './NotePage.scss';

const NotePage = (props) => {
  return <div className='note-page'>NOTE PAGE</div>;
};

NotePage.propTypes = {};

export default NotePage;
