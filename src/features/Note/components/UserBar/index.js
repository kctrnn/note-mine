import React from 'react';
import PropTypes from 'prop-types';

const UserBar = (props) => {
  return <div className='user-bar'>{/* <Logo/> */}</div>;
};

UserBar.propTypes = {};

export default UserBar;
